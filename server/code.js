const fs = require('fs');
const { exec } = require('child_process');

// Assume the user code is written to a file called 'user_code.cpp'
const userCodeFile = 'user_code.cpp';

// Compile the C++ code using the 'g++' compiler

